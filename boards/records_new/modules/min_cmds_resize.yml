---
cases:
  shell.min_cmds_resize:
    path: tests/subsys/shell/shell
    catalog: shell
    extra_args: CONF_FILE=shell_min_cmds_resize.conf
    min_flash: 32
    build_only: true
    min_ram: 32
