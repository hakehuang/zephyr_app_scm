---
cases:
  drivers.led_strip.build:
    path: tests/drivers/build_all/led_strip
    catalog:
    - drivers
    - led_strip
    build_only: true
    min_ram: 32
    depends_on:
    - gpio
    - spi
