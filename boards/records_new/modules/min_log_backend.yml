---
cases:
  shell.min_log_backend:
    path: tests/subsys/shell/shell
    catalog: shell
    extra_args: CONF_FILE=shell_min_log_backend.conf
    min_flash: 64
    build_only: true
    min_ram: 32
