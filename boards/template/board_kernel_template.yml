---
settings:
  case_pipe_name: "${@board}_kernel"
__load__:
- modules/tickless_kernel.yml
- modules/kernel.yml
- skips/${@board}.yml
- boards/${@board}.yml
cases:
  attribute: required
  __common__:
    attribute: required
